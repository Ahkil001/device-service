server:
  port: 8083

spring:
  application:
    name: device-service

  datasource:
    url: jdbc:mysql://localhost:3306/device_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

employee-service:
  url: http://localhost:8082

audit-service:
  url: http://localhost:8084